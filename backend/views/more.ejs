<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>more .ejs page</h1>
<img class="col-4 offset-5" src="<%= el.image %>" alt="Listing Image" width="300">

    <ul>
        <li><h2> title =<%=el.title%></h2></li>
<li><h2>Price = ₹<%= Number(el.price).toLocaleString("en-IN") %></h2></li>
        <li><h2> description =<%=el.description%></h2></li>
        <li><h2> location =<%=el.location%></h2></li>
        <li><h2> Country <%=el.country%></h2></li>
<form method="POST" action="/updatee/<%= el._id %>">
  <button type="submit">Update</button>
</form>
    </ul>
    <hr>
 <div class="col-9 offset-5">
     <h4>Leave review</h4>
     <form method="post" action="/list/<%= el._id %>/review">
       <div>
      <label for="rating">Rating</label>
    <input type="range" min="1" max="5" value="3" step="1" name="review[rating]" required></div>
    <div>
        <label for="comment">Comments</label>
        <textarea name="review[comment]" placeholder="comment pls"required></textarea>
    </div>
      <input class="btn btn-primary" type="submit" value="Submit">
    </form>
      </div>
    <form method="post" action="/list/<%= el._id %>/del?_method=DELETE">      <input class="btn btn-primary" type="submit" value="delte">
</form>
<br>
<h1 class="col-9 offset-5">all reviews</h1>
<!-- <p></p> -->
<% for(let r of el.reviews){ %>
  <div class="card" style="width: 30rem; margin: 10px auto;">
    <div class="card-body">
      <h5 class="card-title">Review</h5>
      <p class="card-text"><%= r.comment %></p>
      <p class="card-text"><strong><%= r.rating %></strong> ★</p>
  

     <form method="POST" action="/list/<%= el._id %>/<%= r._id %>/del?_method=DELETE">
  <input class="btn btn-primary" type="submit" value="Delete">
</form>

    </div>
  </div>
<% } %>


</body>
</html>